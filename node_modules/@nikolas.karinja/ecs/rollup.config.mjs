import { nodeResolve } from '@rollup/plugin-node-resolve'
import { terser } from 'rollup-plugin-terser'

export default {
    input: 'src/index.js',
    output: [
        {
            file: 'build/bundle.mjs',
            format: 'es',
            plugins: [ terser() ],
        },
        {
            file: 'build/bundle.cjs',
            format: 'cjs',
            plugins: [ terser() ],
        }
    ],
    plugins: [ nodeResolve() ],
    external: [ '@nikolas.karinja/app-utils', 'three' ]
}